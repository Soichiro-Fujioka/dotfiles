[pager]
  diff = delta
  log = delta
  reflog = delta
  show = delta

[delta]
  side-by-side = true
  navigate = true

[interactive]
  diffFilter = delta --color-only
[http]
	postBuffer = 524288000
[include]
    path = ~/.config/git/config.local
